TODO:
=====================================================
- que el state funcione con un custom event de tal forma
que cuando se cargue el state se dispare
    - todos los componentes deben escuchar este evento
    de tal forma que solo se inicialice cuando el state
    este listo.
    - esto permite que el state se cargue asincronamente
    sin que los componentes se rompan

- hook para el state?
    - const $state = useState('id_state_element')
    - $state.name = "jhon"

- el html

- el key

- await?
